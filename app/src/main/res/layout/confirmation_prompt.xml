<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="prompt"
            type="com.example.expensetrackerapplication.viewmodel.DeletePromptViewModel" />
    </data>
    <androidx.cardview.widget.CardView
        android:id="@+id/idDeletePromptLayout"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:minWidth="280dp"
        android:maxWidth="320dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="5dp"
        app:cardPreventCornerOverlap="true"
        >
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/idHeader"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@{prompt._title}"
                android:gravity="start|center"
                android:textSize="@dimen/app_header_size"
                android:textColor="?attr/textColorWhite"
                android:background="?attr/colorPrimary"
                android:paddingStart="16dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                />
            <TextView
                android:id="@+id/idContent"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="@{prompt._message}"
                android:textSize="@dimen/text_size"
                app:layout_constraintTop_toBottomOf="@id/idHeader"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                />

            <LinearLayout
                android:id="@+id/idBtnLayout"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:padding="8dp"
                app:layout_constraintTop_toBottomOf="@id/idContent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
>
                <TextView
                    android:id="@+id/idBtnCancel"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_gravity="start"
                    android:layout_weight="1"
                    android:background="?attr/selectableItemBackground"
                    android:gravity="center"
                    android:text="@string/cancel"
                    android:onClick="@{() -> prompt.onClickCancel()}"
                    android:textColor="?attr/colorPrimary"
                    android:textColorLink="?attr/colorPrimary"
                    android:textSize="@dimen/button_text_size"
                    android:textStyle="bold" />

                <Button
                    android:id="@+id/idBtnOk"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:textColor="@color/text_color_white"
                    android:background="@drawable/btn_background"
                    android:text="@string/ok"
                    android:onClick="@{()-> prompt.onClickOk()}"
                    android:textSize="@dimen/button_text_size" />
            </LinearLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>

</layout>